# 工单十三 截图说明

建议最终交付保留以下截图：

## 1. 服务健康检查

- 内容：`GET /api/health` 返回 `{"status":"ok","checks":{"initialized":true,"milvus":"ok"}}`
- 作用：证明后端服务、Milvus 初始化与组件加载正常。

## 2. 单请求阶段拆解

- 内容：执行 `scripts/perf_breakdown.py` 的终端输出
- 建议问题：
  - `公司法定代表人是谁？`
  - `公司的注册资本是多少？`
- 作用：展示 `embedding / vector_search / rerank / total` 的阶段耗时。

## 3. Benchmark 汇总

- 内容：执行 `scripts/perf_benchmark.py` 后的 summary 输出
- 作用：展示 `avg / p50 / p95 / p99 / under_3s_rate`

本次最终建议保留的数字：

- `avg_total_ms = 811.2`
- `p50_total_ms = 499`
- `p95_total_ms = 1933`
- `p99_total_ms = 2185`
- `under_3s_rate = 1.0`

## 4. 并发压测结果

- 内容：执行 `scripts/perf_load_smoke.py` 结果文件或终端摘要
- 配置：`5 并发 / 20 请求`
- 作用：展示轻量并发场景下检索接口的稳定性

建议保留的数字：

- `avg = 447.2`
- `p50 = 400`
- `p95 = 592`
- `p99 = 609`
- `under_3s_rate = 1.0`

## 5. 关键日志证据

- 内容：`app.log` 中检索接口输出的 JSON 行日志
- 作用：证明 `trace_id`、`timings`、`retrieval_time_ms`、`total_time_ms` 已真正落库/落日志。
